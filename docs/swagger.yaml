info:
  contact: {}
paths:
  /publicmessage:
    get:
      consumes:
      - application/json
      description: Returns a sample string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            type: string
      summary: Show a string
      tags:
      - SampleController
swagger: "2.0"
